<% layout("/layout/boilerplate") %>
<body>
    <h2>Your Listings</h2>
    <% for(let listing of listings) {%>
        <h4> <a href="/listing/<%= listing._id %>"><%= listing.title %>  </a></h4>
    <% } %>
</body>